/**
 * Утилиты для работы со строками
 * Содержит общую логику для обработки и санитизации строк
 */

/**
 * Экранирует специальные символы SQL LIKE для безопасного использования в поисковых запросах
 *
 * SQL LIKE использует специальные wildcard символы:
 * - `%` - соответствует любой последовательности символов (0 или более)
 * - `_` - соответствует ровно одному любому символу
 * - `\` - escape символ для экранирования
 *
 * Эта функция экранирует эти символы, чтобы они воспринимались как литералы,
 * а не как wildcard паттерны. Это критично для предотвращения SQL injection
 * и некорректного поведения при поиске строк, содержащих эти символы.
 *
 * @param input - исходная строка для поиска
 * @returns экранированная строка, безопасная для использования в SQL LIKE
 *
 * @example
 * ```typescript
 * escapeLikeWildcards("test%value")   // "test\\%value"
 * escapeLikeWildcards("test_value")   // "test\\_value"
 * escapeLikeWildcards("test\\value")  // "test\\\\value"
 * escapeLikeWildcards("100%")         // "100\\%"
 * ```
 */
export function escapeLikeWildcards(input: string): string {
    // Порядок важен: сначала экранируем backslash, затем остальные символы
    // Иначе уже экранированные символы будут экранированы повторно
    return input
        .replace(/\\/g, '\\\\') // \ -> \\
        .replace(/%/g, '\\%') // % -> \%
        .replace(/_/g, '\\_'); // _ -> \_
}

