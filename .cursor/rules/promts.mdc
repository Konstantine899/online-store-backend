---
description: Главный файл правил проекта - навигация и общие принципы
globs:
alwaysApply: true
---

# Главный файл правил проекта

## Приоритет правил

- **System** > **Developer** > **User** > **docs** (`docs/ai-rules.md`, `rules/*.md`)
- Изменения в код — только после «Одобряю» (READ-ONLY по умолчанию)

## Навигация по правилам

### 🏗️ Архитектура и структура

- **[architecture.md](./architecture.mdc)** - Архитектурные принципы, слои, зависимости
- **[api.md](./api.mdc)** - API правила, Swagger, валидация, пагинация
- **[code-style.md](./code-style.mdc)** - Стиль кода TypeScript/NestJS

### 🗄️ Данные и инфраструктура

- **[db.md](./db.mdc)** - База данных, миграции, сиды
- **[errors-logging.md](./errors-logging.mdc)** - Ошибки, логирование, мониторинг
- **[performance.md](./performance.mdc)** - Производительность, оптимизация

### 🔒 Безопасность и качество

- **[security.md](./security.mdc)** - Безопасность, валидация, авторизация
- **[testing.md](./testing.mdc)** - Тестирование, покрытие, CI/CD

### 🔄 Процессы разработки

- **[git.md](./git.mdc)** - Git, ветвление, коммиты, PR, релизы

## Общие принципы работы с ИИ

### Формат ответов

- **Обязательная структура** (по порядку):
    1. Краткий контекст/цель (1–2 предложения)
    2. Детальный план (нумерованные шаги, кто/что/где, ожидаемый результат)
    3. Риски и альтернативы (когда и что выбрать)
    4. Реализация (при необходимости — код/команды/конфиги)
    5. Проверка и критерии готовности (как убедиться, что всё работает)
    6. Следующие шаги/масштабирование (что сделать дальше)
    7. TL;DR (1–3 пункта квинтэссенции)

### Расширенные правила рабочего ответа

1. Ветвление (название ветки под задачу)

- Формат: `<prefix>/<scope>[-<ticket>]/<kebab-task>[-<extra>]`
- Примеры:
    - `feature/auth/ABC-123/enable-strong-password-validation`
    - `fix/cart/prevent-negative-quantity-on-decrement`
- Выводить название ветки в начале работы над задачей.

2. Показ кода

- По возможности показывать код целиком.
- Если файл большой, показывать код целиком, но разбивать на логические блоки с короткими подзаголовками (например: «Импорты», «Интерфейсы/DTO», «Модель/Схема», «Контроллер», «Сервис», «Тесты»).
- Если вывод физически не помещается, делить на несколько последовательных блоков без пропусков.

3. Комментарии к новому коду

- К каждому блоку нового кода давать краткие комментарии «зачем» (не «что делает»), в 1–3 предложения.
- Не комментировать тривиальные строки; фокус на нетривиальных решениях и компромиссах.

4. Декомпозиция задач (TODO) - АТОМАРНЫЙ ПОДХОД

- Одну большую задачу делить на набор атомарных подзадач (≤ 14 слов каждая).
- Каждая атомарная задача включает ПОЛНЫЙ ЦИКЛ: модель → миграция → сервис → DTO → контроллер → тесты → коммит.
- Всегда формировать TODO перед началом имплементации.
- Один активный пункт in_progress; завершённые пункты отмечать сразу.
- После завершения атомарной задачи - создавать коммит и переходить к следующей.

5. Коммиты

- По завершении каждой атомарной задачи:
    - краткий анализ функциональных изменений (что поменялось для пользователя/системы),
    - предложение коммит-сообщения в формате Conventional Commits: `type(scope): summary`.
- Избегать «мусорных» коммитов; один коммит — одна логическая задача.

6. Ограничения вывода

- Предпочитать целостность кода и связность блоков.
- Если фрагменты зависят друг от друга — выводить их подряд без разрыва.

7. Зарезервировано

- Можно расширить правила: шаблон PR-описаний, формат релизных заметок, автогенерация миграций.

### Работа с кодом

- **Режим только чтения** по умолчанию
- Изменения кода/команд — только после триггера «Одобряю»
- Сначала краткий план и предполагаемые правки
- Соблюдать существующий стиль (линтеры, типы, форматирование)
- Проверять ошибки линтера по изменённым файлам

### Триггеры режимов

#### Режимы планирования (без изменений):

- **«Планируй»** — только план (Контекст → План → Риски → Критерии), без правок/команд
- **«Покажи код»** — показать предлагаемую реализацию (файлы/диффы/команды) без изменений в репозитории и без запуска команд
- **«Объясни»** — детальное объяснение выделенного кода/функции/модуля (flow, зависимости, риски, примеры использования)
- **«Ревью»** — code review текущего файла на best practices, security, performance, архитектуру (без изменений, только рекомендации и приоритизация)
- **«Оптимизируй»** — анализ производительности текущего кода/файла (N+1, индексы, циклы) + конкретные улучшения с оценкой impact (без изменений, показать план)
- **«Исправь баг»** — быстрая диагностика текущей проблемы + план исправления с минимальным планированием (анализ → решение → спросить подтверждение)

#### Режимы выполнения (с изменениями):

- **«Одобряю»** — выполнить строго последний согласованный план, ничего сверх
- **«Коммит»** — оформить текущие изменения в коммит с осмысленным сообщением
- **«Откат»** — вернуть состояние к последнему стабильному безопасным способом
- **«Автофикс»** — автоматически исправить linter/prettier errors (eslint --fix, prettier --write) и закоммитить изменения

### Pre-Commit Best Practices

**ОБЯЗАТЕЛЬНО:** При выполнении задач из `*plan.mdc` файлов, AI должен АВТОМАТИЧЕСКИ следовать `pre_commit_workflow` и `pre_commit_steps` указанным в задаче.

Рекомендуемый workflow ПЕРЕД использованием триггера «Коммит»:

#### Для мелких изменений (<50 строк):

```
«Автофикс» → «Коммит»
```

**Время:** 1-2 минуты

#### Для обычных задач (новые фичи, багфиксы):

```
«Ревью» → «Автофикс» → (запустить тесты вручную) → «Коммит»
```

**Время:** 3-5 минут

#### Для критичных модулей (auth, security, payments, guards):

```
«Ревью» → «Оптимизируй» → «Автофикс» → тесты + coverage → «Коммит»
```

**Время:** 10-15 минут

#### Чек-лист перед коммитом:

- [ ] Код прошёл «Ревью» (для изменений >50 строк)
- [ ] Линтер/prettier исправлены («Автофикс»)
- [ ] Тесты проходят (для новых фич/багфиксов)
- [ ] Coverage не снизился (для критичных модулей)
- [ ] Коммит-сообщение осмысленное (Conventional Commits)

### Ограничения

- Без отдельного разрешения: ≤3 файла или ≤50 строк
- Не менять публичные контракты/зависимости
- При неоднозначности — явно сформулировать допущения

## Исследование и планирование

- Для поиска по коду — семантический поиск, затем точечные чтения
- На задачи >1 шага — создавать TODO-список с атомарными пунктами
- Один активный пункт in_progress; отмечать выполненные сразу

## Безопасность/конфиденциальность

- Не раскрывать секреты, токены, PII
- Маскировать чувствительные данные
- Не генерировать бинарные/длинные хэши

## Атомарный подход к разработке

### Принципы атомарных задач

- **Одна задача = один коммит** - каждая атомарная задача завершается коммитом
- **Полный цикл разработки** - от модели до тестов включительно
- **Независимость** - каждая задача может быть разработана и протестирована отдельно
- **Инкрементальность** - постепенное наращивание функциональности

### Структура атомарной задачи

1. **Анализ** - что именно добавляем/изменяем
2. **Миграция** - изменения в БД (если нужны)
3. **Модель** - обновление Domain модели
4. **Сервис** - бизнес-логика
5. **DTO** - валидация входных данных
6. **Контроллер** - API endpoints
7. **Тесты** - unit + integration тесты
8. **Коммит** - фиксация изменений

### Примеры атомарных задач

- `feat(user): add phone field to user profile`
- `feat(user): add newsletter subscription flag`
- `feat(user): add user address management`
- `feat(product): add SKU field to products`
- `feat(cart): add guest cart support`

## Что избегать

- Лишняя болтовня, дублирование, длинные теории без практики
- Глобальные рефакторинги без запроса
- Переизобретение стиля проекта
- Большие монолитные задачи без разбиения на атомарные
