# AI Workflow Rules (Global)

## Execution Policy - ОБЯЗАТЕЛЬНО для всех \*.plan.mdc

Эти правила применяются ко ВСЕМ задачам из \*.plan.mdc файлов.

### 1. Commit Approval (Подтверждение коммита)

```yaml
commit_approval_required: true
```

**Правило:**

- ❌ НИКОГДА не делать `git commit` автоматически
- ✅ ВСЕГДА спрашивать: **«Коммит?»** или **«Выполнить коммит изменений?»**
- ✅ Показать предложенное commit message
- ✅ Дождаться явного подтверждения от пользователя («да», «Одобряю», «Коммит»)

**Исключения:** НЕТ. Даже для тривиальных изменений.

---

### 2. Pre-commit Steps Strict (Строгое выполнение)

```yaml
pre_commit_steps_strict: true
```

**Правило:**

- ❌ НЕЛЬЗЯ пропускать шаги из `pre_commit_steps`
- ❌ НЕЛЬЗЯ менять порядок шагов
- ❌ НЕЛЬЗЯ выполнять "по своему усмотрению"
- ✅ ОБЯЗАТЕЛЬНО выполнять ВСЕ шаги СТРОГО по списку
- ✅ Если шаг не применим - явно сказать почему и спросить подтверждение

**Порядок выполнения:**

1. Выполнить шаг 1 из `pre_commit_steps`
2. Показать результат
3. Выполнить шаг 2
4. И т.д. до конца списка
5. Только после ВСЕХ шагов - спросить подтверждение коммита

---

### 3. Show File Links Before Commit (Ссылки на файлы)

```yaml
show_file_links_before_commit: true
```

**Правило:**

- ✅ ПЕРЕД вопросом «Коммит?» показать список изменённых файлов
- ✅ Ссылки должны быть кликабельными (относительные пути)
- ✅ Указать тип изменения: NEW, MODIFIED, DELETED
- ✅ Краткое описание изменений в каждом файле

**Формат:**

```markdown
## 📁 Изменённые файлы (для вашей проверки):

### Modified:

1. [`path/to/file.ts`](path/to/file.ts)
    - Краткое описание изменений

### Created:

2. [`path/to/new-file.ts`](path/to/new-file.ts) ✨ NEW
    - Что содержит новый файл

### Deleted:

3. `path/to/deleted-file.ts` 🗑️
    - Почему удалён
```

**После списка файлов:**

```markdown
---

**Проверьте файлы выше перед коммитом.**
**Хотите внести правки?** Скажите какие, или ответьте **«Коммит»** для продолжения.
```

---

### 4. Pre-commit Workflow Types

Каждая задача должна иметь один из типов:

#### **ТЩАТЕЛЬНЫЙ** (критичные модули: auth, security, guards, payments)

```yaml
pre_commit_steps:
    - '«Ревью» → security, best practices, error handling'
    - '«Оптимизируй» → N+1, async operations, performance'
    - '«Автофикс» → linter/prettier (npm run lint:ts:fix)'
    - 'Тесты: npm test -- <test-pattern>'
    - 'Coverage: проверить метрики (npm run test:cov)'
    - 'Показать файлы: список изменений с описанием'
    - 'Спросить: «Коммит?»'
    - '«Коммит» → <conventional-commit-message>'
```

#### **СТАНДАРТНЫЙ** (бизнес-логика, конфигурация)

```yaml
pre_commit_steps:
    - '«Ревью» → business logic, edge cases'
    - '«Автофикс» → linter/prettier'
    - 'Тесты: npm test -- <test-pattern>'
    - 'Показать файлы: список изменений'
    - 'Спросить: «Коммит?»'
    - '«Коммит» → <conventional-commit-message>'
```

#### **ОБЛЕГЧЁННЫЙ** (документация, мелкие правки)

```yaml
pre_commit_steps:
    - '«Ревью» → документация актуальна, форматирование'
    - '«Автофикс» → prettier для markdown'
    - 'Показать файлы: список изменений'
    - 'Спросить: «Коммит?»'
    - '«Коммит» → <conventional-commit-message>'
```

---

### 5. File Review Format (Обязательный формат)

**ДО коммита ВСЕГДА показывать:**

```markdown
## 📁 Файлы для проверки (TEST-XXX):

### 📝 Modified (изменённые):

1. [`src/path/to/file.ts`](src/path/to/file.ts)
    - **Что изменено:** Добавлен метод X, исправлена логика Y
    - **Строки:** +50, -20
    - **Почему:** Реализация feature Z согласно DoD

2. [`config/file.config.ts`](config/file.config.ts)
    - **Что изменено:** Обновлены thresholds
    - **Строки:** +10, -5

### ✨ Created (новые):

3. [`docs/NEW-DOC.md`](docs/NEW-DOC.md)
    - **Содержит:** Документация по feature X
    - **Строки:** +200
    - **Цель:** Comprehensive guide для пользователей

### 🗑️ Deleted (удалённые):

4. `old/deprecated-file.ts`
    - **Причина:** Заменён на новый подход

---

## 📊 Статистика:

- **Total files:** 4 (2 modified, 1 new, 1 deleted)
- **Lines:** +260, -25

---

**Проверьте файлы выше.**
**Хотите внести свои правки?** Скажите какие.
**Готовы к коммиту?** Ответьте **«Коммит»** или **«Одобряю коммит»**.
```

---

## 🔧 Что добавляю в каждый \*.plan.mdc:

```yaml
execution_policy:
    must_follow: true
    allow_code_changes_only_after: 'Одобряю <TASK_ID>'

    # NEW RULES:
    commit_approval_required: true # ← НОВОЕ: спрашивать перед коммитом
    pre_commit_steps_strict: true # ← НОВОЕ: строго следовать шагам
    show_file_links_before_commit: true # ← НОВОЕ: показывать файлы ДО коммита

    workflow_rules:
        - 'Выполнять ВСЕ pre_commit_steps СТРОГО по порядку'
        - 'ПЕРЕД коммитом: показать список файлов с описанием изменений'
        - 'ВСЕГДА спрашивать: «Коммит?» или «Готовы к коммиту?»'
        - "Не коммитить без явного подтверждения: 'Коммит', 'да', 'Одобряю коммит'"
```

---

Начинаю обновление всех 30 файлов. **Это займёт несколько минут.**

**Одобряю начать массовое обновление?**
