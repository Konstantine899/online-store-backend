# Test Coverage для SaaS продуктов

## Почему высокий coverage критичен для SaaS

### 💰 Бизнес-причины

**Репутация = Revenue**:

- Один критичный баг → отток клиентов → потеря MRR
- Downtime стоит реальных денег (SLA penalties)
- Негативные отзывы влияют на новые продажи

**Юридические обязательства**:

- SLA commitments перед клиентами (99.9% uptime)
- Данные клиентов (GDPR compliance)
- Финансовые транзакции (PCI DSS для payments)
- Audit trails и логирование

**Конкурентное преимущество**:

- "Professionally tested" в маркетинговых материалах
- Доверие enterprise клиентов
- Готовность к security audits
- Certification ready (ISO 27001, SOC 2)

---

## Coverage Metrics Explained

### 1. Branches Coverage (ветвления)

```typescript
// Каждое if/else/switch создаёт ветки
if (user.isActive) {
    // ← Ветка 1: true
    return 'active';
} else {
    // ← Ветка 2: false
    return 'inactive';
}

// 70% branches = нужно протестировать 7 из 10 веток
```

**Что покрывается**:

- ✅ if/else statements
- ✅ switch/case branches
- ✅ Ternary operators (? :)
- ✅ Logical operators (&& ||)

### 2. Functions Coverage (функции)

```typescript
function login() {} // ← Функция 1
function logout() {} // ← Функция 2
function refresh() {} // ← Функция 3

// 70% functions = нужно вызвать 7 из 10 функций
```

### 3. Lines Coverage (строки кода)

```typescript
1: const name = "John";   // ← Строка 1
2: const age = 30;        // ← Строка 2
3: console.log(name);     // ← Строка 3

// 75% lines = 75% строк выполнились в тестах
```

### 4. Statements Coverage (выражения)

```typescript
const a = 1; // ← Statement 1
const b = 2; // ← Statement 2
return a + b; // ← Statement 3

// 75% statements = 75% выражений выполнились
```

---

## Целевые метрики по типу проекта

| Тип проекта                | Branches   | Functions  | Lines      | Обоснование                    |
| -------------------------- | ---------- | ---------- | ---------- | ------------------------------ |
| **Open Source библиотека** | 85-95%     | 85-95%     | 90-98%     | Публичный API, много users     |
| **Banking/FinTech**        | 80-90%     | 80-90%     | 85-92%     | Деньги, регуляторы, аудит      |
| **SaaS продукт**           | **70-80%** | **70-80%** | **75-85%** | **← ВАШ СЛУЧАЙ**               |
| **Enterprise backend**     | 60-70%     | 60-70%     | 65-75%     | Internal, контролируемая среда |
| **Startup MVP**            | 40-50%     | 40-50%     | 45-55%     | Скорость важнее coverage       |
| **Prototype**              | 20-30%     | 20-30%     | 25-35%     | Proof of concept               |

---

## Per-Module Coverage Strategy

### CRITICAL Modules (85%+ coverage)

**Почему**: Баги здесь = критичные проблемы безопасности/доступности

```javascript
'./src/infrastructure/services/auth/**/*.ts': {
    branches: 85,
    functions: 85,
    lines: 90,
    statements: 90
}

'./src/infrastructure/common/guards/**/*.ts': {
    branches: 85,
    functions: 85,
    lines: 90,
    statements: 90
}

'./src/infrastructure/common/strategies/**/*.ts': {
    branches: 85,
    functions: 85,
    lines: 90,
    statements: 90
}
```

**Модули**:

- ✅ Authentication (login, registration, password reset)
- ✅ Authorization (guards, strategies, RBAC)
- ✅ Payment processing
- ✅ Security (rate limiting, validation, sanitization)

---

### HIGH Priority Modules (75-80% coverage)

**Почему**: Баги здесь = проблемы user experience/данные

```javascript
'./src/infrastructure/services/user/**/*.ts': {
    branches: 80,
    functions: 80,
    lines: 85,
    statements: 85
}

'./src/infrastructure/services/order/**/*.ts': {
    branches: 75,
    functions: 75,
    lines: 80,
    statements: 80
}
```

**Модули**:

- ✅ User management
- ✅ Order processing
- ✅ Cart operations
- ✅ Notifications

---

### MEDIUM Priority Modules (65-70% coverage)

**Почему**: Важно, но не критично

```javascript
'./src/infrastructure/controllers/**/*.ts': {
    branches: 70,
    functions: 70,
    lines: 75,
    statements: 75
}

'./src/infrastructure/services/**/*.ts': {
    branches: 65,
    functions: 65,
    lines: 70,
    statements: 70
}
```

**Модули**:

- Controllers (тонкий слой)
- Generic services
- Repositories

---

### EXCLUDED (0% coverage required)

**Почему**: Не имеет смысла тестировать напрямую

```javascript
'./src/**/*.dto.ts': { /* all: 0 */ }
'./src/**/*.response.ts': { /* all: 0 */ }
'./src/**/*.interface.ts': { /* all: 0 */ }
'./src/**/*.module.ts': { /* all: 0 */ }
```

**Типы файлов**:

- DTO классы (тестируются через validation)
- Response types (тестируются через API)
- Interfaces (TypeScript compile-time)
- Module definitions (DI configuration)

---

## Закон убывающей отдачи

```
Coverage    Усилия       ROI (польза/затраты)
──────────────────────────────────────────────
0-30%       Низкие       ⭐⭐⭐⭐⭐ Критичные пути
30-50%      Средние      ⭐⭐⭐⭐ Основной функционал
50-70%      Высокие      ⭐⭐⭐ Edge cases
70-85%      Огромные     ⭐⭐ Редкие сценарии
85-95%      Безумные     ⭐ Граничные условия
95-100%     Невозможно   - Не стоит затрат
```

**Правило**: 70% → 85% требует **в 3-5 раз больше** работы чем 50% → 70%

---

## Unit vs Integration Coverage

### Unit Tests (с моками)

**Coverage**: 20-30% от общего кода

**Почему низкий**:

- Тестируют только логику метода
- Не покрывают integration paths
- Не тестируют DB/HTTP layers

**Пример**:

```typescript
it('should calculate total', () => {
    const service = new OrderService(mockRepo);
    expect(service.calculateTotal([item1])).toBe(100);
});
```

**Что покрывается**: только `calculateTotal()` метод

---

### Integration Tests (реальная БД + HTTP)

**Coverage**: 40-60% от общего кода

**Почему выше**:

- Весь стек: Controller → Service → Repository → DB
- Реальные HTTP requests
- Валидация, guards, pipes
- Error handling

**Пример**:

```typescript
it('should create order', async () => {
    const res = await request(app)
        .post('/orders')
        .set('Authorization', `Bearer ${token}`)
        .send(orderData)
        .expect(201);
});
```

**Что покрывается**:

- ✅ Controller endpoint
- ✅ Auth guard
- ✅ Role guard
- ✅ DTO validation
- ✅ Service business logic
- ✅ Repository DB query
- ✅ Response serialization

---

## Что НЕ нужно тестировать

### 1. Declarative Code (декларативный код)

```typescript
// DTO classes - только декларация
export class CreateUserDto {
    @IsString()
    @IsEmail()
    email: string; // ← Не тестируется напрямую
}

// Тестируется косвенно через API validation tests
```

### 2. Framework Code (код фреймворка)

```typescript
// NestJS decorators - тестируется фреймворком
@Controller('user')
@ApiTags('Users')
export class UserController {}
```

### 3. Configuration Files

```typescript
// Конфигурация - проверяется в runtime
export const config = {
    port: 5000,
    host: 'localhost',
};
```

### 4. Type Definitions

```typescript
// Интерфейсы - compile-time проверки
interface IUserService {
    createUser(dto: CreateUserDto): Promise<User>;
}
```

**Итого**: ~30-40% кода не требует прямого тестирования!

---

## Test Isolation Best Practices

### Проблема: Race Conditions

```typescript
// ❌ ПЛОХО - тесты влияют друг на друга
it('test 1', async () => {
    await UserModel.create({ email: 'test@test.com' });
});

it('test 2', async () => {
    const user = await UserModel.findOne({ email: 'test@test.com' });
    // ← test@test.com может быть от test 1 или других тестов!
});
```

### Решение 1: Транзакции

```typescript
// ✅ ХОРОШО - полная изоляция
describe('User Tests', () => {
    let transaction: Transaction;

    beforeEach(async () => {
        transaction = await sequelize.transaction();
    });

    afterEach(async () => {
        await transaction.rollback();
    });

    it('test 1', async () => {
        await UserModel.create(data, { transaction });
        // После теста - автоматически откатится
    });
});
```

### Решение 2: Unique Test Data

```typescript
// ✅ ХОРОШО - каждый тест свои данные
const uniqueEmail = `test-${Date.now()}-${Math.random()}@example.com`;
await UserModel.create({ email: uniqueEmail });
```

### Решение 3: Cleanup

```typescript
// ✅ ХОРОШО - явная очистка
afterEach(async () => {
    await UserModel.destroy({ where: { email: testEmail } });
    await OrderModel.destroy({ where: { userId: testUserId } });
});
```

---

## SaaS-Specific Test Scenarios

### Security Tests (обязательно!)

```typescript
describe('Security', () => {
    it('should block brute force login attempts');
    it('should prevent SQL injection in search');
    it('should sanitize XSS in user inputs');
    it('should validate file upload types');
    it('should rate limit API requests');
});
```

### Multi-Tenancy Tests (если применимо)

```typescript
describe('Tenant Isolation', () => {
    it('TENANT_ADMIN cannot access other tenant data');
    it('should filter data by tenant_id automatically');
    it('should prevent cross-tenant resource access');
});
```

### Data Integrity Tests

```typescript
describe('Data Integrity', () => {
    it('should rollback on payment failure');
    it('should prevent negative balances');
    it('should maintain referential integrity');
    it('should handle concurrent updates correctly');
});
```

### Compliance Tests

```typescript
describe('GDPR Compliance', () => {
    it('should export user data on request');
    it('should anonymize user on deletion');
    it('should log consent changes');
});
```

---

## Coverage Tools & CI Integration

### Local Development

```bash
# Полный coverage report
npm run test:cov
npm run test:cov:open  # Открыть HTML отчёт

# Coverage по типу тестов
npm run test:cov:unit
npm run test:cov:integration

# Watch mode с coverage
npm run test:watch -- --coverage
```

### CI/CD Pipeline

```yaml
# GitHub Actions
- name: Run tests with coverage
  run: npm run test:cov:integration

- name: Check coverage thresholds
  run: |
      if ! npm run test:cov:integration; then
        echo "❌ Coverage threshold not met"
        exit 1
      fi

- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
      files: ./coverage/lcov.info
```

### Coverage Badges

```markdown
# README.md

[![Coverage](https://codecov.io/gh/org/repo/branch/main/graph/badge.svg)](https://codecov.io/gh/org/repo)

# Для SaaS показывает профессионализм
```

---

## Типичные ошибки и решения

### ❌ Ошибка 1: Тесты ради coverage

```typescript
// ❌ ПЛОХО - бесполезный тест
it('should have property name', () => {
    expect(user).toHaveProperty('name');
});
```

```typescript
// ✅ ХОРОШО - тест проверяет логику
it('should validate email format', async () => {
    await expect(service.createUser({ email: 'invalid' })).rejects.toThrow(
        'Неверный формат email',
    );
});
```

### ❌ Ошибка 2: Игнорирование edge cases

```typescript
// ❌ Тестируем только happy path
it('should create order', () => {
    const order = await service.create(validData);
    expect(order).toBeDefined();
});
```

```typescript
// ✅ Тестируем edge cases
it('should reject negative quantity');
it('should reject out-of-stock products');
it('should handle concurrent order creation');
it('should rollback on payment failure');
```

### ❌ Ошибка 3: Flaky tests

```typescript
// ❌ ПЛОХО - зависит от времени
it('should expire token', async () => {
    await sleep(1001); // Может упасть в медленном CI
    expect(token.isExpired()).toBe(true);
});
```

```typescript
// ✅ ХОРОШО - детерминированно
it('should expire token', async () => {
    const expiredToken = createToken({ expiresAt: Date.now() - 1000 });
    expect(expiredToken.isExpired()).toBe(true);
});
```

---

## Coverage vs Quality

**Coverage НЕ равно Quality!**

```
❌ 90% coverage с плохими тестами < ✅ 60% coverage с качественными тестами
```

### Признаки качественных тестов:

✅ **Понятные названия**:

```typescript
it('should reject login after 5 failed attempts'); // ✅ Ясно
vs;
it('test bruteforce'); // ❌ Непонятно
```

✅ **Тестируют поведение, не реализацию**:

```typescript
it('should send welcome email on registration'); // ✅ Behaviour
vs;
it('should call emailService.send()'); // ❌ Implementation
```

✅ **Независимые и изолированные**:

```typescript
// ✅ Каждый тест независим
beforeEach(() => {
    // Fresh setup
});
```

✅ **Покрывают edge cases**:

```typescript
// ✅ Не только happy path
it('should handle valid input');
it('should reject invalid input');
it('should handle concurrent access');
it('should recover from failures');
```

---

## Continuous Improvement Strategy

### Текущая ситуация → Целевая

```
Week 0:  47% coverage  →  Week 9:  75% coverage
         335 tests              469 tests
         15 failed              0 failed
```

### Incremental Approach

**Не пытайтесь достичь 70% за раз!**

1. **Week 1-2**: Исправить broken tests (47% → 50%)
2. **Week 3-4**: Критичные модули (50% → 60%)
3. **Week 5**: RBAC (60% → 65%)
4. **Week 6-7**: Business logic (65% → 72%)
5. **Week 8**: Configuration (72% → 75%)
6. **Week 9**: E2E flows (75%)

**Каждую неделю**: +5-10% coverage, стабильный прогресс

---

## Monitoring & Metrics

### Обязательные метрики для SaaS:

```bash
# Локально - каждый день
npm run test:cov

# В CI - каждый PR
- name: Check coverage
  run: npm run test:cov:integration
```

### Dashboard метрики:

- **Test count**: тренд роста (335 → 469)
- **Pass rate**: должен быть 100%
- **Coverage %**: тренд роста (47% → 75%)
- **Test duration**: не должна расти (оптимизация)
- **Flaky rate**: должна быть 0%

### Alerts:

```yaml
# Упал coverage
if coverage < previous_coverage - 2%:
    block_pr: true
    message: 'Coverage снизился на ${delta}%'

# Появились failed tests
if failed_tests > 0:
    block_pr: true
    message: '${failed_tests} tests failing'

# Flaky tests
if test_retry_count > 0:
    warn: true
    message: 'Flaky test detected: ${test_name}'
```

---

## Рекомендации для команды

### Code Review чеклист:

**Для нового кода**:

- [ ] Добавлены unit тесты для новой логики
- [ ] Добавлены integration тесты для новых endpoints
- [ ] Покрыты edge cases и error scenarios
- [ ] Coverage не упал (или обоснован временный спад)
- [ ] Тесты стабильные (прошли 3+ раза)

**Для багфиксов**:

- [ ] Добавлен regression test (воспроизводит баг)
- [ ] Test проходит после фикса
- [ ] Coverage улучшен или не хуже

### Definition of Done:

Код считается готовым когда:

1. ✅ Все линтеры проходят
2. ✅ Все тесты проходят (0 failed)
3. ✅ Coverage ≥ threshold
4. ✅ CI pipeline green
5. ✅ PR approved by 2+ reviewers

---

## Инструменты и библиотеки

### Текущий стек:

```json
{
    "jest": "^29.x", // Test runner
    "supertest": "^6.x", // HTTP assertions
    "@nestjs/testing": "^10.x", // NestJS test utilities
    "ts-jest": "^29.x" // TypeScript transform
}
```

### Рекомендуемые дополнения:

```json
{
    "@faker-js/faker": "^8.x", // Test data generation
    "jest-retry": "^1.x", // Retry flaky tests (временно)
    "codecov": "^3.x" // Coverage reporting
}
```

---

## Справочные материалы

**Внешние ресурсы**:

- [Jest Best Practices](https://github.com/goldbergyoni/javascript-testing-best-practices)
- [NestJS Testing Documentation](https://docs.nestjs.com/fundamentals/testing)
- [Martin Fowler - Test Pyramid](https://martinfowler.com/bliki/TestPyramid.html)

**Внутренние документы**:

- `.cursor/rules/testing.mdc` - общие правила тестирования
- `.cursor/rules/testing/testing.coverage.plan.mdc` - план улучшения coverage
- `docs/ci-cd.mdc` - CI/CD процессы

---

**Version**: 1.0
**Last Updated**: 2025-10-08
**Maintainer**: Development Team
