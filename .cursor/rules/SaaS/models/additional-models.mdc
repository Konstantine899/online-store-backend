# Дополнительные модели для полноценного ecommerce

## Анализ текущего состояния

### ✅ Уже есть в проекте:
- **User** - пользователи
- **Role** - роли пользователей  
- **UserRole** - связь пользователей и ролей
- **Category** - категории товаров
- **Brand** - бренды
- **Product** - товары
- **ProductProperty** - свойства товаров
- **Rating** - рейтинги товаров
- **Order** - заказы
- **OrderItem** - товары в заказе
- **Cart** - корзина
- **CartProduct** - товары в корзине
- **RefreshToken** - токены обновления

### 🆕 Критичные модели (уже в планах):
- **Payment** - система платежей
- **Inventory** - управление складом
- **Discount** - система скидок
- **Shipping** - доставка
- **Technical Models** - аудит, логирование, кэширование
- **Analytics** - аналитика

## 🔍 Дополнительные модели для полноценного ecommerce

### 1. Система уведомлений
```typescript
// notification.mdc
interface NotificationSystem {
    notifications: Notification[];
    notificationTemplates: NotificationTemplate[];
    notificationSettings: NotificationSettings;
    notificationLogs: NotificationLog[];
}

interface Notification {
    id: number;
    userId: number;
    type: NotificationType;
    title: string;
    message: string;
    data?: any;
    status: NotificationStatus;
    sentAt?: Date;
    readAt?: Date;
    createdAt: Date;
}

enum NotificationType {
    EMAIL = 'email',
    SMS = 'sms',
    PUSH = 'push',
    IN_APP = 'in_app',
    WEBHOOK = 'webhook'
}

enum NotificationStatus {
    PENDING = 'pending',
    SENT = 'sent',
    DELIVERED = 'delivered',
    READ = 'read',
    FAILED = 'failed'
}
```

### 2. Система возвратов и обменов
```typescript
// return-exchange.mdc
interface ReturnExchangeSystem {
    returns: Return;
    returnItems: ReturnItem;
    returnReasons: ReturnReason;
    exchanges: Exchange;
    refunds: Refund;
}

interface Return {
    id: number;
    orderId: number;
    userId: number;
    returnNumber: string;
    reason: ReturnReason;
    status: ReturnStatus;
    requestedAt: Date;
    approvedAt?: Date;
    receivedAt?: Date;
    processedAt?: Date;
    refundAmount: number;
    notes?: string;
}

enum ReturnStatus {
    REQUESTED = 'requested',
    APPROVED = 'approved',
    REJECTED = 'rejected',
    RECEIVED = 'received',
    PROCESSED = 'processed',
    REFUNDED = 'refunded'
}
```

### 3. Система лояльности и рефералов
```typescript
// loyalty-referral.mdc
interface LoyaltyReferralSystem {
    loyaltyPrograms: LoyaltyProgram;
    loyaltyTiers: LoyaltyTier;
    loyaltyTransactions: LoyaltyTransaction;
    referrals: Referral;
    referralRewards: ReferralReward;
}

interface LoyaltyProgram {
    id: number;
    name: string;
    description: string;
    isActive: boolean;
    rules: LoyaltyRule[];
    tiers: LoyaltyTier[];
}

interface Referral {
    id: number;
    referrerId: number;
    referredId: number;
    referralCode: string;
    status: ReferralStatus;
    rewardAmount: number;
    createdAt: Date;
}
```

### 4. Система подписок и уведомлений
```typescript
// subscription.mdc
interface SubscriptionSystem {
    subscriptions: Subscription;
    subscriptionPlans: SubscriptionPlan;
    subscriptionFeatures: SubscriptionFeature;
    subscriptionUsage: SubscriptionUsage;
}

interface Subscription {
    id: number;
    userId: number;
    planId: number;
    status: SubscriptionStatus;
    startDate: Date;
    endDate?: Date;
    autoRenew: boolean;
    paymentMethodId: number;
    createdAt: Date;
}

enum SubscriptionStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    CANCELLED = 'cancelled',
    EXPIRED = 'expired',
    SUSPENDED = 'suspended'
}
```

### 5. Система отзывов и комментариев
```typescript
// review-comment.mdc
interface ReviewCommentSystem {
    reviews: Review;
    reviewComments: ReviewComment;
    reviewLikes: ReviewLike;
    reviewReports: ReviewReport;
    reviewModeration: ReviewModeration;
}

interface Review {
    id: number;
    productId: number;
    userId: number;
    orderId?: number;
    rating: number;
    title: string;
    content: string;
    pros?: string[];
    cons?: string[];
    images: string[];
    isVerified: boolean;
    status: ReviewStatus;
    helpfulCount: number;
    createdAt: Date;
}

enum ReviewStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
    HIDDEN = 'hidden'
}
```

### 6. Система сравнения товаров
```typescript
// product-comparison.mdc
interface ProductComparisonSystem {
    comparisons: ProductComparison;
    comparisonItems: ComparisonItem;
    comparisonAttributes: ComparisonAttribute;
}

interface ProductComparison {
    id: number;
    userId?: number;
    sessionId?: string;
    name?: string;
    products: ComparisonItem[];
    attributes: ComparisonAttribute[];
    createdAt: Date;
}
```

### 7. Система избранного (Wishlist)
```typescript
// wishlist.mdc
interface WishlistSystem {
    wishlists: Wishlist;
    wishlistItems: WishlistItem;
    wishlistShares: WishlistShare;
}

interface Wishlist {
    id: number;
    userId: number;
    name: string;
    isPublic: boolean;
    isDefault: boolean;
    items: WishlistItem[];
    createdAt: Date;
}
```

### 8. Система купонов и промокодов
```typescript
// coupon-promo.mdc
interface CouponPromoSystem {
    coupons: Coupon;
    couponUsage: CouponUsage;
    promoCampaigns: PromoCampaign;
    promoRules: PromoRule;
}

interface Coupon {
    id: number;
    code: string;
    name: string;
    description: string;
    type: CouponType;
    value: number;
    minOrderAmount?: number;
    maxDiscount?: number;
    usageLimit?: number;
    usageCount: number;
    validFrom: Date;
    validTo: Date;
    isActive: boolean;
    applicableProducts?: number[];
    applicableCategories?: number[];
    applicableUsers?: number[];
}
```

### 9. Система управления контентом
```typescript
// content-management.mdc
interface ContentManagementSystem {
    pages: Page;
    pageBlocks: PageBlock;
    banners: Banner;
    banners: Banner;
    news: News;
    faq: FAQ;
}

interface Page {
    id: number;
    title: string;
    slug: string;
    content: string;
    metaTitle?: string;
    metaDescription?: string;
    isActive: boolean;
    isPublic: boolean;
    template: string;
    blocks: PageBlock[];
    createdAt: Date;
}
```

### 10. Система интеграций
```typescript
// integration.mdc
interface IntegrationSystem {
    integrations: Integration;
    integrationLogs: IntegrationLog;
    webhooks: Webhook;
    apiKeys: ApiKey;
}

interface Integration {
    id: number;
    name: string;
    type: IntegrationType;
    provider: string;
    config: any;
    isActive: boolean;
    lastSync?: Date;
    createdAt: Date;
}

enum IntegrationType {
    PAYMENT = 'payment',
    SHIPPING = 'shipping',
    EMAIL = 'email',
    SMS = 'sms',
    ANALYTICS = 'analytics',
    CRM = 'crm',
    ERP = 'erp'
}
```

### 11. Система мультиязычности
```typescript
// localization.mdc
interface LocalizationSystem {
    languages: Language;
    translations: Translation;
    localizedContent: LocalizedContent;
}

interface Language {
    id: number;
    code: string;
    name: string;
    nativeName: string;
    isActive: boolean;
    isDefault: boolean;
    sortOrder: number;
}

interface Translation {
    id: number;
    languageId: number;
    key: string;
    value: string;
    context?: string;
    createdAt: Date;
}
```

### 12. Система управления файлами
```typescript
// file-management.mdc
interface FileManagementSystem {
    files: File;
    fileCategories: FileCategory;
    filePermissions: FilePermission;
}

interface File {
    id: number;
    filename: string;
    originalName: string;
    mimeType: string;
    size: number;
    path: string;
    url: string;
    category: FileCategory;
    uploadedBy: number;
    isPublic: boolean;
    createdAt: Date;
}
```

## Приоритизация дополнительных моделей

### 🔴 Критичные для MVP
1. **Notification** - уведомления пользователей
2. **Return/Exchange** - возвраты и обмены
3. **Review/Comment** - отзывы и комментарии

### 🟡 Важные для роста
4. **Loyalty/Referral** - программа лояльности
5. **Wishlist** - избранное
6. **Product Comparison** - сравнение товаров
7. **Coupon/Promo** - купоны и промоакции

### 🟢 Дополнительные функции
8. **Subscription** - подписки
9. **Content Management** - управление контентом
10. **Integration** - внешние интеграции
11. **Localization** - мультиязычность
12. **File Management** - управление файлами

## Рекомендации по внедрению

### Фаза 1: Базовый функционал (MVP)
- Notification
- Return/Exchange
- Review/Comment

### Фаза 2: Маркетинг и лояльность
- Loyalty/Referral
- Wishlist
- Coupon/Promo

### Фаза 3: Расширенный функционал
- Product Comparison
- Subscription
- Content Management

### Фаза 4: Интеграции и масштабирование
- Integration
- Localization
- File Management

## Заключение

Эти дополнительные модели превратят ваш ecommerce в полноценную платформу с:
- ✅ Полным циклом работы с клиентами
- ✅ Маркетинговыми инструментами
- ✅ Системой лояльности
- ✅ Управлением контентом
- ✅ Интеграциями с внешними сервисами
- ✅ Мультиязычной поддержкой

Начните с критичных моделей для MVP, а затем постепенно добавляйте остальные по мере роста бизнеса.