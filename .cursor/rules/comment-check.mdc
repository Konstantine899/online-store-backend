# Проверка комментариев

## Назначение
Проверка комментариев в коде на предмет выявления "нейросетевого" стиля написания.

## Триггер
**«Проверь_Комментарии»** - активирует режим проверки комментариев

## Алгоритм проверки

### 1. Поиск комментариев
- Найти все комментарии в указанном файле
- Разделить на типы: `//`, `/* */`, `/** */`

### 2. Анализ стиля комментариев

#### 🚨 Признаки "нейросетевого" стиля:

**A. Избыточные пояснения:**
- Объяснение очевидных вещей: `// 1000 * 2`, `// i++`
- Математические вычисления: `// цена * количество`
- Очевидные операции: `// получение данных`, `// сохранение`

**B. Формальные структуры:**
- Пошаговые комментарии: `// Шаг 1:`, `// Этап 2:`
- Слишком детальные описания простых операций
- Комментарии-инструкции: `// Сначала делаем это, потом то`

**C. Шаблонные тестовые фразы:**
- `// Arrange`, `// Act`, `// Assert`
- `// Given`, `// When`, `// Then`
- `// Act & Assert`, `// Arrange & Act`
- `// Подготовка данных`
- `// Выполнение операции`
- `// Проверка результата`
- `// Настройка моков`
- `// Вызов метода`
- `// Ожидание результата`
- `// Проверка вызова`
- `// Очистка ресурсов`
- `// Завершение теста`
- `// Создание объекта`
- `// Установка значений`
- `// Получение результата`
- `// Сравнение значений`

**D. Избыточная документация:**
- Комментарии к каждой строке
- Объяснение стандартных паттернов
- Дублирование названий переменных

**E. Математические пояснения:**
- `// 1000 * 2` (очевидные вычисления)
- `// цена * количество`
- `// сумма + налог`
- `// процент / 100`
- `// количество - 1`
- `// размер / 1024`
- `// время + интервал`
- `// вес * коэффициент`
- `// площадь * высота`
- `// скорость * время`

**F. Пошаговые инструкции:**
- `// Шаг 1:`, `// Шаг 2:`, `// Шаг 3:`
- `// Этап 1:`, `// Этап 2:`, `// Этап 3:`
- `// Первый шаг:`, `// Второй шаг:`
- `// Сначала:`, `// Затем:`, `// Наконец:`
- `// Начало:`, `// Середина:`, `// Конец:`

**G. Избыточные объяснения очевидного:**
- `// Получение корзины` (очевидно из названия метода)
- `// Добавление товара` (очевидно из названия метода)
- `// Увеличение количества` (очевидно из названия метода)
- `// Удаление элемента` (очевидно из названия метода)
- `// Создание пользователя` (очевидно из названия метода)
- `// Обновление профиля` (очевидно из названия метода)
- `// Поиск по ID` (очевидно из названия метода)
- `// Сохранение в БД` (очевидно из контекста)
- `// Загрузка данных` (очевидно из контекста)
- `// Отправка запроса` (очевидно из контекста)


### 3. Классификация комментариев

#### ✅ Нормальные комментарии:
- Описание бизнес-логики
- Объяснение сложных алгоритмов
- Предупреждения о побочных эффектах
- TODO/FIXME с конкретными задачами
- ESLint disable с обоснованием

#### ⚠️ Подозрительные комментарии:
- Объяснение простых операций
- Математические вычисления
- Пошаговые инструкции
- Шаблонные фразы

#### 🚨 Явно "нейросетевые":
- Комментарии типа "Было/Стало"
- Избыточные объяснения очевидного
- Формальные структуры без смысла

### 4. Отчет о проверке

#### Формат отчета:
```
## Результат проверки комментариев

### Найденные комментарии: X штук
- Нормальные: Y штук
- Подозрительные: Z штук  
- Проблемные: N штук

### Детальный анализ:
[Список проблемных комментариев с номерами строк]

### Рекомендации:
[Конкретные предложения по исправлению]
```

## Примеры

### ❌ Плохие комментарии:
```typescript
// Получение корзины
const cart = await getCart();

// Добавление товара
cart.addProduct(product);

// Увеличение количества
cart.increment(productId);

// Математическое вычисление
price: 2000, // 1000 * 2

// Шаблонные тестовые комментарии
// Arrange
const mockUser = createMockUser();

// Act
const result = await service.createUser(mockUser);

// Assert
expect(result).toBeDefined();

// Пошаговые инструкции
// Шаг 1: Подготовка данных
const data = prepareData();

// Шаг 2: Выполнение операции
const result = processData(data);

// Шаг 3: Проверка результата
validateResult(result);

// Избыточные объяснения очевидного
// Создание пользователя
const user = await userService.createUser(userData);

// Обновление профиля
await userService.updateProfile(userId, profileData);

// Поиск по ID
const user = await userService.findById(userId);
```

### ✅ Хорошие комментарии:
```typescript
// Кэшируем корзину для оптимизации производительности
const cart = await getCart();

// Обрабатываем особый случай для товаров с нулевой ценой
if (product.price === 0) {
    // TODO: Добавить валидацию для бесплатных товаров
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
// Необходимо для тестирования приватных методов
const privateMethod = (service as any).privateMethod;
```

## Интеграция с существующими правилами

### Связь с другими режимами:
- **«Планируй»** - может включать проверку комментариев в план
- **«Одобряю»** - должен учитывать результаты проверки комментариев
- **«Коммит»** - может требовать исправления комментариев перед коммитом

### Приоритет проверки:
1. **Критично** - явно "нейросетевые" комментарии
2. **Важно** - подозрительные комментарии
3. **Рекомендуется** - нормальные комментарии

## Автоматизация

### Команды для проверки:
```bash
# Проверка конкретного файла
npm run lint:ts src/path/to/file.ts

# Поиск комментариев
grep -n "//" src/path/to/file.ts
grep -n "/\*" src/path/to/file.ts
```

### Интеграция с CI/CD:
- Добавить проверку комментариев в pipeline
- Блокировать merge при наличии проблемных комментариев
- Генерировать отчеты о качестве комментариев